Things to Test
--------------------------------

AddExpr (PASSES)

AndExpr
- boolean z = x && y
- if (x && y)

ArrayExpr (PASSES)

AssignStmt (PASSES)

BlockStmt (PASSES)

CallExpr

ClassDecl (FAILS)

DivExpr (PASSES)

EqualExpr (PASSES)

FieldExpr (PASSES)

GreaterExpr (PASSES)

IfStmt (FAILS)

LesserExpr (PASSES)

MethodDecl (PASSES)

MulExpr (PASSES)

NegExpr (PASSES)

NewArrayExpr (PASSES)

NewObjectExpr (PASSES)

NotEqExpr (PASSES)

NotExpr

OrExpr (PASSES)

SubExpr (PASSES)

ThreadDecl

VarDecl (PASSES)

VoidDecl

WhileStmt (doesn't match Brylow's, might be okay)

XinuCallExpr (works)

XinuCallStmt (works)
