Things to Test
--------------------------------

AddExpr (PASSES)

AndExpr
- boolean z = x && y
- if (x && y)

ArrayExpr (PASSES)

AssignStmt (PASSES)

BlockStmt (PASSES)

CallExpr

ClassDecl (FAILS)

DivExpr (PASSES)

EqualExpr (PASSES)

FieldExpr (PASSES)

GreaterExpr (PASSES)

IfStmt (FAILS)

LesserExpr (PASSES)

MethodDecl

MulExpr

NegExpr

NewArrayExpr

NewObjectExpr

NotEqExpr

NotExpr

OrExpr

SubExpr

ThreadDecl

VarDecl

VoidDecl

WhileStmt

XinuCallExpr (works)

XinuCallStmt (works)
