Things to Test
--------------------------------

AddExpr (PASSES)

AndExpr
- boolean z = x && y (PASSES)
- if (x && y) (FAILS)

ArrayExpr (PASSES)

AssignStmt (PASSES)

BlockStmt (PASSES)

CallExpr (PASSES)

ClassDecl (PASSES)

DivExpr (PASSES)

EqualExpr (PASSES)

FieldExpr (PASSES)

GreaterExpr (PASSES)

IfStmt (PASSES)

LesserExpr (PASSES)

MethodDecl (PASSES)

MulExpr (PASSES)

NegExpr (PASSES)

NewArrayExpr (PASSES)

NewObjectExpr (PASSES)

NotEqExpr (PASSES)

NotExpr (PASSES)

OrExpr (PASSES)

SubExpr (PASSES)

ThreadDecl (PASSES)

VarDecl (PASSES)

VoidDecl (brylow's ReadAbsyn doesn't like VoidDecls, can't test)

WhileStmt (PASSES)

XinuCallExpr (PASSES)

XinuCallStmt (PASSES)
