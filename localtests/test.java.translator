DataFrag(
  .data
notExpr_vtable:
  .word notExpr.main

)
DataFrag(
  .data
LS_vtable:
  .word LS.Start

)
ProcFrag(
MipsFrame(
main:
Formals(InReg(t32)
		InReg(t33)
		)
Actuals(InReg(t4)
		InReg(t5)
		)
BadPtr(_BADPTR)
BadSub(_BADSUB)
)
SEQ(
 EXP(
  CALL(
   NAME(_printint)
   ESEQ(
    SEQ(
     MOVE(
      TEMP(t34)
      CALL(
       NAME(_new)
       CONST(0)
       NAME(LS_vtable)
      ))
     SEQ(
      CJUMP(EQ
       TEMP(t34)
       CONST(0)
       _BADPTR L0)
      LABEL(L0)))
    CALL(
     MEM(
      BINOP(PLUS
       MEM(
        BINOP(PLUS
         TEMP(t34)
         CONST(-4)))
       CONST(0)))
     TEMP(t34)
     CONST(10)
    ))
  ))
 MOVE(
  TEMP(t2)
  CONST(0)))
)
ProcFrag(
MipsFrame(
LS.Start:
Formals(InReg(t35)
		InReg(t36)
		)
Actuals(InReg(t4)
		InReg(t5)
		)
BadPtr(_BADPTR)
BadSub(_BADSUB)
)
SEQ(
 SEQ(
  MOVE(
   TEMP(t37)
   CONST(0))
  SEQ(
   SEQ(
    CJUMP(NE
     BINOP(BITXOR
      CONST(1)
      CONST(1))
     CONST(0)
     L1 L2)
    SEQ(
     SEQ(
      LABEL(L1)
      MOVE(
       TEMP(t37)
       CONST(17)))
     JUMP(
      NAME(L2))))
   LABEL(L2)))
 MOVE(
  TEMP(t2)
  TEMP(t37)))
)
